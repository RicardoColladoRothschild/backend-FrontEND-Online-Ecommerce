{
    "rewrites":[
        {
            "source":"/api/(.*)",
            "destination":"/api/$1"
        }
    ],
    "builds":[
        {
            "src": "/api/index.js",
            "use": "@vercel/node"
        }
    ]
}
