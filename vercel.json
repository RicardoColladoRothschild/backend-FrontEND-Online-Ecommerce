{
    "rewrites":[
        {
            "source":"/api/(.*)",
            "destination":"/api"
        }
    ],
    "builds":[
        {
            "src": "/api/app.js",
            "use": "@vercel/node"
        }
    ]
}
